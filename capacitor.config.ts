import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'alten-technical-test',
  webDir: 'src',
  server: {
    androidScheme: 'https'
  }
};

export default config;
